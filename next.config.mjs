/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['7x8dq6l6vi.ufs.sh'], // Allow external video/image sources
  },
};

export default nextConfig;
