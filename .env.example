# Example environment variables for frontend (Vite)
# Set `VITE_API_BASE` to the full backend base URL (no trailing slash recommended).
# If left empty or omitted, the frontend will use a same-origin relative path (`/api/...`).

# Example: backend hosted separately
VITE_API_BASE=https://api.yourdomain.com

# Example: backend served from same origin (leave blank or omit)
# VITE_API_BASE=
